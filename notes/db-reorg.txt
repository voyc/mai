12 apr 2020
executed against thaidict--------------------
alter table mai.thaidict add column tlm char(1);
alter table mai.thaidict add column cpm char(1);
update mai.thaidict set components = concat_ws(',', leadingconsonant, vowelpattern, finalconsonant, tonemark, tone) where type = 'o';
update mai.thaidict set cpm = 'm' where type='o';

13 apr 2020 
create dict and mean tables, split from thaidict
see sql/db-reorg.sql

todo---------------------------

new svc
	parsestring
	parsestory
		support meanings available/chosen for each word
	getdict (lookup)
	setdict (insert/update)
		call parsestring for cp when g=m
	setstory (insert/update)
		call parsestory
	getstory (return parsed object, html ready?)
	getstorylist

refresh client functions
	parse syllable

new client
	lookup story
	edit story
	after parse story, let user resolve multiple meanings

cli
	recalc and reset level of every word/meaning
		count word usage in all stories
		including components


